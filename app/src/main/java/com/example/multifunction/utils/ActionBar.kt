package com.example.multifunction.utils

import android.app.Activity
import android.content.Intent
import com.example.multifunction.modulos.BroadcastData

class ActionBar(var activity: Activity) {

    fun somar(){
        var intent: Intent
//        intent?.action = BroadcastData.BROADCAST_SOMA.toString()
//        activity.sendBroadcast(intent);
    }
}